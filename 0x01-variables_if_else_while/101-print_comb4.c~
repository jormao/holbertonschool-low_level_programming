#include <stdio.h>
/**
 * main - prints all possible different combinations of three digits
 *
 * Return: cero
 */
int main(void)
{
	int i, c = 49;
	int j = 48;
	int k, d, e = 50;
	int a = 44;
	int b = 32;
	/*solo se puede usar 6 putchar*/
	for (; j < 56; j++)
	{
		for (; i <= 57; i++)
		{
			for (; k <= 57; k++)
			{
				putchar(j);
				putchar(i);
				putchar(k);
				if ((j == 55) && (i == 56) && (k == 57))
					break;
				putchar(a);
				putchar(b);
			}
			d++;
			k = d;
		}
		e++;
		k = e;
		c++;
		i = c;
	}
	putchar('\n');
	return (0);
}
