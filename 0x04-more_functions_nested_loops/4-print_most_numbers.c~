#include "holberton.h"
/**
 * print_most_numbers - prints the numbers from 0 to 9
 *
 * Return: always 0
 */

void print_most_numbers(void)
{
	int i;

	/*print 0-9 without 2 y 4 using only _putchar */
	for (i = '0'; i <= '9'; i++)
	{
		if (i != '2' && i != '4')
		_putchar(i);
	}
	_putchar('\n');
}
